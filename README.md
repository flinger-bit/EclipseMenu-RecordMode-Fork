# EclipseMenu Record Mode Fork

This fork isolates the bot recording flow into two modes:

- Normal recording
- Frame-stepper-safe recording

The project is structured for Geode and keeps the feature set focused on the bot tab and replay pipeline.
